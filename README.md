# TKP Frontend

## 使用

```bash
cd tkp-platform-frontend
npm install
npm run dev
```

默认开发地址：`http://127.0.0.1:5173`

## 环境变量

复制 `.env.example` 为 `.env`：

```bash
cp .env.example .env
```

当前支持：
- `VITE_API_BASE_URL`：后端 API 基地址
